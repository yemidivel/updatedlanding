/**
 * SellSync Configuration
 *
 * After deploying the backend to Vercel, replace the empty string
 * below with your backend URL, e.g. "https://sellsync-api.vercel.app"
 */

const CONFIG = {
  API_BASE_URL: "https://sellsync-backend.vercel.app",
  FRONTEND_URL: "",
};
